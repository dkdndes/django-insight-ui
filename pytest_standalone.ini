[tool:pytest]
python_files = test_playwright_standalone.py
addopts = 
    --tb=short
    --strict-markers
    --disable-warnings
    -v
markers =
    slow: marks tests as slow
    ui: marks tests as UI tests
